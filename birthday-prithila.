<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

<title>Happy Birthday PRITHILA 🎀</title>

<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:'Poppins',sans-serif;}
html,body{width:100%;height:100%;overflow-x:hidden;background:#0b0014;color:white;}

body::before{
content:"";
position:fixed;
inset:0;
background:radial-gradient(circle at top,#ff4da6,#0b0014);
z-index:-1;
}

/* Banner */
.banner{
text-align:center;
padding:40px 10px;
font-size:40px;
font-weight:bold;
animation:glow 2s infinite alternate;
}
@keyframes glow{
from{text-shadow:0 0 10px pink;}
to{text-shadow:0 0 30px hotpink;}
}

/* Floating hearts */
.heart{
position:fixed;
bottom:-20px;
font-size:20px;
animation:float 6s linear infinite;
}
@keyframes float{
to{transform:translateY(-120vh);opacity:0;}
}

/* Card */
.card{
background:rgba(255,255,255,0.1);
margin:20px;
padding:25px;
border-radius:25px;
backdrop-filter:blur(8px);
box-shadow:0 0 20px rgba(255,105,180,.4);
}

/* Countdown */
#countdown{
text-align:center;
font-size:22px;
margin-top:10px;
}

/* Gallery */
.gallery{
display:grid;
grid-template-columns:repeat(auto-fit,minmax(140px,1fr));
gap:12px;
margin-top:15px;
}
.gallery img{
width:100%;
height:180px;
object-fit:cover;
border-radius:18px;
animation:fadeZoom 1.5s ease;
}
@keyframes fadeZoom{
from{opacity:0;transform:scale(.8);}
to{opacity:1;transform:scale(1);}
}

/* Popup */
#popup{
position:fixed;
inset:0;
background:rgba(0,0,0,.8);
display:none;
justify-content:center;
align-items:center;
z-index:999;
}
#popupContent{
background:#ff2f92;
padding:30px;
border-radius:25px;
text-align:center;
width:90%;
max-width:360px;
box-shadow:0 0 30px white;
animation:pop 1s ease;
}
@keyframes pop{
from{transform:scale(.5);opacity:0;}
to{transform:scale(1);opacity:1;}
}

/* Fireworks canvas */
canvas{
position:fixed;
inset:0;
pointer-events:none;
z-index:998;
}

/* Mobile */
@media(max-width:600px){
.banner{font-size:30px;}
p{font-size:15px;}
}
</style>
</head>

<body>

<canvas id="fireworks"></canvas>

<div class="banner">🎉 Happy Birthday PRITHILA 🎀</div>

<div class="card">
<p>
Happy Birthday my LOVE ❤️ my CUTIE 💋 my SWEETHEART 🦋 my DARLING 👀  
my PRETTIEST WIFEY 🎀  
<br><br>
Every day with you is a gift 🥺 I cherish every laugh, every moment, every memory with you 💖  
You are my best friend, my home, my forever 😭❤️  
<br><br>
I LOVE YOU Jaanpakhiii ❤️❤️❤️
</p>
</div>

<div class="card">
<h2 style="text-align:center;">⏳ Countdown to 25 May</h2>
<div id="countdown"></div>
</div>

<div class="card">
<h2 style="text-align:center;">📸 Our Memories</h2>
<div class="gallery">
<img src="img1.jpg">
<img src="img2.jpg">
<img src="img3.jpg">
<img src="img4.jpg">
<img src="img5.jpg">
<img src="img6.jpg">
</div>
</div>

<div id="popup">
<div id="popupContent">
<h2>🎂 Happy Birthday PRITHILA 🎆</h2>
<p>You are the most beautiful part of my life 💖</p>
</div>
</div>

<script>
// Floating hearts
setInterval(()=>{
const h=document.createElement("div");
h.className="heart";
h.innerHTML="❤️";
h.style.left=Math.random()*100+"vw";
h.style.fontSize=(15+Math.random()*20)+"px";
document.body.appendChild(h);
setTimeout(()=>h.remove(),6000);
},500);

// Countdown
const birthday=new Date("2026-05-25 00:00:00").getTime();
setInterval(()=>{
const now=new Date().getTime();
const d=birthday-now;
if(d<=0){
document.getElementById("countdown").innerHTML="🎉 TODAY IS THE DAY 🎉";
document.getElementById("popup").style.display="flex";
launchFireworks();
}else{
const days=Math.floor(d/(1000*60*60*24));
const hrs=Math.floor((d%(1000*60*60*24))/(1000*60*60));
const min=Math.floor((d%(1000*60*60))/(1000*60));
document.getElementById("countdown").innerHTML=
`${days}d ${hrs}h ${min}m`;
}
},1000);

// Fireworks
const canvas=document.getElementById("fireworks");
const ctx=canvas.getContext("2d");
canvas.width=innerWidth;canvas.height=innerHeight;
function launchFireworks(){
for(let i=0;i<200;i++){
setTimeout(()=>{
ctx.fillStyle=`hsl(${Math.random()*360},100%,60%)`;
ctx.beginPath();
ctx.arc(Math.random()*canvas.width,Math.random()*canvas.height,2,0,Math.PI*2);
ctx.fill();
},i*20);
}
}
</script>

</body>
</html>
